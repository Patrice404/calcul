int i = 0;
while (i < 10) {
    i = i + 1;
    if (i % 2 != 0) then {  // Si impair
        continue;      // Passe au prochain tour de boucle
    }
    output(i);          // Affiche seulement les pairs
}
