int x;
int y;

int pgcd(int a, int b){
    while(True){

        int temp = a;
        if (b==0) then break;
        a = b;
        b = temp % b;

    }
        
    return a;
}

input(x);
input(y);

output(pgcd(x, y));
